# UI

```bash
npm install
npm run dev
```

## Chrome for Testing

```bash
# Install Chrome for Testing
npx @puppeteer/browsers install chrome@stable

# Launch
npx @puppeteer/browsers launch chrome

# Launch with command-line arguments 
npx @puppeteer/browsers launch chrome -- --disable-infobars
```
